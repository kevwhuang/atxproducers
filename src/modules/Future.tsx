import React from 'react';

import '../styles/modules/Future.scss';

function Future(): React.ReactElement {
    return (
        <section className="future">
        </section>
    );
}

export default Future;
