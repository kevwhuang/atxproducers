import React from 'react';

import '../styles/modules/Feature.scss';

function Feature(): React.ReactElement {
    return (
        <section className="feature">
        </section>
    );
}

export default Feature;
