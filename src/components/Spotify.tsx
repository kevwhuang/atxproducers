import React from 'react';

import '../styles/components/Spotify.scss';

function Spotify(): React.ReactElement {
    return (
        <section className="spotify">
        </section>
    );
}

export default Spotify;
