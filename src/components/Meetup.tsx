import React from 'react';

import '../styles/components/Meetup.scss';

function Meetup(): React.ReactElement {
    return (
        <section className="meetup">
        </section>
    );
}

export default Meetup;
